#pragma once

// WiFi credentials — copy this file to config.h and fill in your values
#define WIFI_SSID "YourNetwork"
#define WIFI_PASS "YourPassword"

// Mac relay server address (run: ipconfig getifaddr en0)
#define RELAY_HOST "192.168.1.XXX"
#define RELAY_PORT 8080
